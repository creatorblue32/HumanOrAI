"use client";

import React, { useEffect, useState } from 'react';




const Page = () => {

  return (
    <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: '100vh' }}>
        Unfortunately, this device is not compatible with this system.

        If it's enabled, try turning off Incognito mode. If that doesn't work, then you may not be able to connect. 
    </div>

  );
};

export default Page;


